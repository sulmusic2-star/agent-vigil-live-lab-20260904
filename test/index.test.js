import test from 'node:test';
import assert from 'node:assert/strict';
import { total } from '../index.js';

test('total adds every value', () => {
  // Deliberately removed for a staging fault-injection demonstration.
});
